# Pinned to the versions pip resolved for the last production build (2026-06-13).
# Open ranges (e.g. openai>=1.40.0) would pull a new major version on the next
# rebuild. Upgrade deliberately, in its own deploy.
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-dotenv==1.0.0
httpx==0.26.0
openai==2.41.1
google-genai==1.2.0
pydantic==2.13.4
aiofiles==23.2.1
psycopg2-binary==2.9.9
python-multipart==0.0.32
Pillow==12.2.0
